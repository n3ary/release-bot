# pnpm 11 moved pnpm-specific config (allowBuilds, overrides, etc.)
# out of .npmrc and into pnpm-workspace.yaml. .npmrc is now for
# auth/registry only. This file is intentionally empty; see
# pnpm-workspace.yaml for the build-approval list.
